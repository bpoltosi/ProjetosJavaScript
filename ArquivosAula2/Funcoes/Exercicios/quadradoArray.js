/* MAPEIA um array de numeros para um array de numeros elevados a potencia 2 */

let numeros = [1, 2, 3, 4, 5];
let resto = numeros.map((n) => Math.pow(n, 2));

console.log(numeros);
console.log(resto);
